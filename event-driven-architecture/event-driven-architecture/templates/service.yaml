apiVersion: v1
kind: Service
metadata:
  name: {{ include "event-driven-architecture.fullname" . }}
  labels:
    {{- include "event-driven-architecture.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "event-driven-architecture.selectorLabels" . | nindent 4 }}
